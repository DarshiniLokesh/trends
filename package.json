{
  "name": "trends",
  "private": true,
  "engines": { "node": "22.x" },
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.1"
  }
}
